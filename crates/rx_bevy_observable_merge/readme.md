# rx_bevy_observable_merge

## Example

```sh
cargo run -p rx_bevy_observable_merge --features example --example merge_example
```

```sh
cargo run -p rx_bevy_observable_merge --features example --example merge_subject_example
```
