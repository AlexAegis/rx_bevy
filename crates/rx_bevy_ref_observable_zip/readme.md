# rx_bevy_ref_observable_zip

## Example

```sh
cargo run -p rx_bevy_ref_observable_zip --features example --example zip_example
```

```sh
cargo run -p rx_bevy_ref_observable_zip --features example --example zip_subject_example
```

```sh
cargo run -p rx_bevy_ref_observable_zip --features example --example zip_backpressure_example
```
